## Bugs

- Show full path plus file name on the top of the editor.
- Shared view allign left, which should be centered. perhaps centering is not from theme forger but added in the preview locally, thus in shared view it should be added separately as well. 
- '.settings' folder and subfolders duplication. 
- Share folder
    - raw works but the file name starts with 'ðŸ“„'. 
    - rendered does not render the preview but show file names in grid view. Should show a side panel to list all files and a right panel to show the preview of selected file. Previes here should be the same as preview when editing the file. 
- Markdown preview, whether split mode, preview mode or shared mode, should inject the same content into the container including the html elements and styling. Should not have two separate files 'MarkdownPreview.tsx' and 'MarkdownSplitEditor.tsx'. 
## Features

- Separate the guest access from the login card to a separate card, so it is easier for users to find it. -done
- Use accent color and artistic font for title 'ORZ Editor'. -done